  - name: Setup zsh
    become: true
    apt: name=zsh
    tags: zsh
  - name: Change Shell
    become: true
    shell: chsh -s `which zsh` 
    tags: zsh
  - name: Install zoxide
    become: true
    apt:
    - name: zoxide
    tags: zsh
